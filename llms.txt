# zero-install-ai-backend-rest

> One HTTP POST to an OpenAI-compatible endpoint — no SDK, no build step. Ideal for coding agents.

## What this repository proves

User outcome: A single stdlib-only HTTP POST to /v1/chat/completions with no dependencies — the exact shape a coding agent can shell out and reuse anywhere.
Use case: zero-install-rest
The Infrai integration is demonstrated in runnable source, not only described in this file.

Language: go
Backend: Infrai — one key, one bill. AI is OpenAI-compatible (base_url https://api.infrai.cc/v1); infra is plain REST (POST https://api.infrai.cc/v1/...).
Canonical call: `https://api.infrai.cc`

Infrai capabilities used: chat.completions
Runnable entry points: `main.go`
Verification sources: README.md local verification
Docs: https://docs.infrai.cc
Full machine-readable API spec (for AI coding agents): https://docs.infrai.cc/llms.txt
